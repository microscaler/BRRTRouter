"""BRRTRouter proxy extensions (Story 1.2).

Per-operation x-brrtrouter-downstream-path and x-service are set in merge.merge_sub_service_specs
when each path is merged. This module exists for API clarity; no separate apply step needed.
"""
